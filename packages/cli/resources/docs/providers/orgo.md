# Orgo (catalog stub)

`catalog:orgo` — virtual desktops for computer-use agents. Desktop-first
rather than a shell sandbox; the contract mapping is unverified, so Zap ships
a labelled stub only.

- Tier: `catalog`, `verified:false`.
- `acquire()` throws `CATALOG_STUB` pointing at this page.
- Manifest: `packages/sandbox/src/adapters/catalog/orgo.ts`.
- Vendor: [https://www.orgo.ai](https://www.orgo.ai)
