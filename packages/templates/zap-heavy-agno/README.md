# zap-heavy-agno

Opt-in overlay of `zap-heavy` (no named snapshot). The manifest lives at
`packages/runtime/src/harness/agno.ts`.

See `docs/templates/zap-heavy-agno.md` and `docs/harnesses/agno.md`.
