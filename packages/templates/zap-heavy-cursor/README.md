# zap-heavy-cursor

Opt-in overlay of `zap-heavy` (no named snapshot). The manifest lives at
`packages/runtime/src/harness/cursor.ts`.

See `docs/templates/zap-heavy-cursor.md` and `docs/harnesses/cursor.md`.
