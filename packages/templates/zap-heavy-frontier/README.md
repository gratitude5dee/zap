# zap-heavy-frontier

Opt-in overlay of `zap-heavy` (no named snapshot). The manifest lives at
`packages/runtime/src/harness/frontier.ts`.

See `docs/templates/zap-heavy-frontier.md` and `docs/harnesses/frontier.md`.
