# zap-heavy-grok

Overlay of `zap-heavy-opencode` (no named snapshot) that makes the xAI route
the default LLM. Doctor output states the xAI-routed status. The manifest
lives at `packages/runtime/src/harness/grok.ts`.

See `docs/templates/zap-heavy-grok.md` and `docs/harnesses/grok.md`.
