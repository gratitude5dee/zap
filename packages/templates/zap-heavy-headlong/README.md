# zap-heavy-headlong

Opt-in overlay of `zap-heavy` (no named snapshot). The manifest lives at
`packages/runtime/src/harness/headlong.ts`.

See `docs/templates/zap-heavy-headlong.md` and `docs/harnesses/headlong.md`.
