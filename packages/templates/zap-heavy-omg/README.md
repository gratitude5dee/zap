# zap-heavy-omg

Overlay of `zap-heavy` (no named snapshot) that installs `@omg-dev/cli` at
the pinned version via bun and drives `omg computer` over `ws-jsonrpc`.
The manifest lives at `packages/runtime/src/harness/omg.ts`.

See `docs/templates/zap-heavy-omg.md` and `docs/harnesses/omg.md`.
