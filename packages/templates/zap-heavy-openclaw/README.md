# zap-heavy-openclaw

Named snapshot built from `zap-heavy` that installs `openclaw` at the pinned
version and drives its chat-completions endpoint over `openai-compat`.
The manifest lives at `packages/runtime/src/harness/openclaw.ts`.

See `docs/templates/zap-heavy-openclaw.md` and `docs/harnesses/openclaw.md`.
