# zap-heavy-prime

Opt-in overlay of `zap-heavy` (no named snapshot). The manifest lives at
`packages/runtime/src/harness/prime.ts`.

See `docs/templates/zap-heavy-prime.md` and `docs/harnesses/prime.md`.
