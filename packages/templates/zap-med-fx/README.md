# zap-med-fx

Overlay of `zap-med` (no named snapshot) that installs the fx CLI and drives
it as `fx ask --json` per turn. The harness manifest lives at
`packages/runtime/src/harness/fx.ts` (`run: "cli-exec"`).

See `docs/templates/zap-med-fx.md`.
