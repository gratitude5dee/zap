# Zap Registry

This local registry contains portable Zap skills that can be installed with:

```bash
zap add zap-world-cup-entrance
zap add zap-caught-by-the-cam
```

Each entry is a complete `agent/skills/zap-*` directory.
