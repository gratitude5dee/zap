# zap

Use this skill when working in a Zap project.

## Rules

- Treat `Zap.md` as the recipe source of truth.
- Keep prompt files beside the recipe that references them.
- Run `zap validate` and `zap lint` before committing recipe changes.
- Default to dry-run planning unless the user explicitly approves live provider spend.
